{
  "name": "flight-project-sample",
  "version": "0.0.0",
  "devDependencies": {
    "generator-flight": "~0.4.1",
    "karma": "~0.8.5"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --single-run --browsers PhantomJS"
  }
}
